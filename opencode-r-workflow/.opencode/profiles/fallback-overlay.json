{
  "$schema": "https://opencode.ai/config.json",

  "//1": "Fallback overlay for when opencode-go quota is exhausted.",
  "//2": "Loaded via OPENCODE_CONFIG_CONTENT by scripts/opencode-fallback,",
  "//3": "which puts it at the top of opencode's config precedence — it",
  "//4": "overrides the `model:` field of every agent's .md frontmatter,",
  "//5": "but merges everything else (prompts, tools, caching) untouched.",
  "//6": "",
  "//7": "Only the architect needs an override — it is the only agent using",
  "//8": "opencode-go. The four DeepSeek-family agents already go direct via",
  "//9": "the deepseek provider in both modes; nothing changes for them.",
  "//10": "",
  "//11": "IMPORTANT — verify the OpenRouter Kimi K3 model ID against the",
  "//12": "current catalog before first use, since OpenRouter's slugs shift:",
  "//13": "    opencode models openrouter --refresh",

  "agent": {
    "architect": { "model": "openrouter/moonshotai/kimi-k3" }
  }
}
